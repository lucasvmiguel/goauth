package user

import (
	"github.com/stretchr/testify/assert"
	"testing"
)

func TestGenerateUser(t *testing.T) {
	//user1 := UserAuth{}
	//user2 := UserAuth(gin.context)
	//assert.Equal(t, user1, user2, "they should not be equal")
	assert.Equal(t, 9, 7, "they should be equal")
}
